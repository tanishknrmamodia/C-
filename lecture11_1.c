/*
switch statments

switch (expression)
{
case (constant-expression ):
    ( code )
    break;

default:   default is used when none of case gets matched
    break;
}

rules for switch
1. expression must be in or char 
2. case value must be an int or char 
3. case must come inside switch
4. break is not a must
*/
